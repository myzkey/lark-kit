{
  "name": "@lark-kit/example-lambda",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node18 --outfile=dist/index.js --format=esm --external:@aws-sdk/*"
  },
  "dependencies": {
    "lark-kit": "workspace:*"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "esbuild": "^0.19.10",
    "typescript": "^5.3.2"
  }
}
